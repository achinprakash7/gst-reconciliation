# Disable the embedded web server ? this is a desktop app
spring.main.web-application-type=none

# Suppress the Spring Boot banner in the terminal
spring.main.banner-mode=off

# Keep startup logging minimal
logging.level.root=WARN
logging.level.com.powerloom=INFO